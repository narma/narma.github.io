# The URL the site will be built for
base_url = "https://narma.github.io"

title = "Sergey Rublev"
description = "λ"
default_language = "en"
generate_rss = true
theme = "hyde"

# Whether to automatically compile all Sass files in the sass directory
compile_sass = true

[markdown]
highlight_code = true
highlight_theme = "nord"

# Whether to build a search index to be used later on by a JavaScript library
build_search_index = false

[slugify]
paths = "on"
taxonomies = "off"
anchors = "on"

[extra.social]
github = "narmaru"
twitter = "narmaru"

